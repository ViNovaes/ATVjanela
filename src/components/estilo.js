import React from "react";
import { StyleSheet } from "react-native";

export default fundo = StyleSheet.create(
    {
        geral:{
            backgroundColor: `#e79827`,
            flexGrow: 1,
            // alignItems:`center`
            justifyContent:`center`
        }
    }
)